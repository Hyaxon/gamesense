[project]
name = "gamesense-prediction"
version = "0.1.0"
description = "Prediction service for the GameSense application"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
]
